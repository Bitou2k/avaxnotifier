const getStartMessage = () => `This bot will notify you automatically when a new cycle is eligible for payout

For payout check you can type /payout

For specific cycle, specify cycle number e.g. /payout 200

To repeat this message you can type /help
`

module.exports = {
  getStartMessage,
}
